export interface BoxInterface {
   // box: {
        post_id: number,
        title: string,
        type: string,
        language: string,
        replyNum: number,
        likeNum: number,
        countView: number,
        tags: string[]
   // }[]
}